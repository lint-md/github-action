name: build

on: [ pull_request ]

jobs:
  unit-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6.0.2

      - name: Install modules
        run: npm ci

      - name: Run tests
        run: npm test